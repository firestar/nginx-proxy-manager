#!/command/with-contenv bash
# shellcheck shell=bash

set -e

. /usr/bin/common.sh

log_info 'Starting npm-agent ...'

while :
do
	s6-setuidgid "$PUID:$PGID" bash -c "node --abort_on_uncaught_exception /app/agent/index.js"
	sleep 1
done
